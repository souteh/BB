<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/black">

        <ImageView
            android:id="@+id/left_btn_toolbar"
            android:layout_width="@dimen/toolbar_btn_width"
            android:layout_height="@dimen/toolbar_btn_height"
            android:layout_alignParentStart="true"
            android:layout_centerVertical="true"
            android:padding="@dimen/toolbar_btn_padding"/>

        <TextView
            android:id="@+id/title_toolbar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:layout_toStartOf="@id/right_btn_toolbar"
            android:layout_toEndOf="@id/left_btn_toolbar"
            android:gravity="center"
            style="@style/TextStyleTitleToolbar"/>

        <ImageView
            android:id="@+id/right_btn_toolbar"
            android:layout_width="@dimen/toolbar_btn_width"
            android:layout_height="@dimen/toolbar_btn_height"
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"
            android:padding="@dimen/toolbar_btn_padding" />

    </RelativeLayout>
</LinearLayout>